# DevOps Infrastructure Project

## What it does
This is a simple Flask application that returns "Hello, Docker!" when accessed.

## How it works
The application is dockerized using a Dockerfile. The Flask application is served in a Docker container, and the application is accessible on port 80.

## How to run
You need Docker installed on your machine to run this project. Once Docker is installed, follow these steps:

1. Build the Docker image: